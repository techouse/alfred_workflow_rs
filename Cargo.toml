[package]
name = "alfred_workflow_rs"
version = "0.1.0"
edition = "2024"

[dependencies]
cached = { version = "0.55", default-features = false, features = ["disk_store"] }
md5 = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"

[dev-dependencies]
pretty_assertions = "1.4"
tempfile = "3.10"
